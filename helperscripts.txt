visualization:
    python tools/visualizations/vf_progress.py

run tests:
    cd .\apps\api\
    pytest -v

run app:
    Backend (FastAPI)

    cd apps/api

    # Create/activate virtual environment (Windows)
    python -m venv .venv
    .venv\Scripts\activate

    # Install dependencies
    pip install -r requirements.txt

    # Create .env with your OpenAI key
    echo OPENAI_API_KEY=your_key_here > .env

    # Start the server
    uvicorn vibeforge_api.main:app --reload --port 8000

    API will be at http://localhost:8000 with docs at http://localhost:8000/docs

    UI (React + Vite)

    cd apps/ui

    # Install dependencies
    npm install

    # Start dev server
    npm run dev

    UI will typically run on http://localhost:5173
    

todooo:
    silleen et input idea.md luodaan jonkun queryn kautta
    esim "5 featurea" päätyy promptiin "the user wants to implement in total [n_of_features] in total. They are [eka] [toka] jne

    okei nyt siis workflow on tämmönen:
    - Muodosta idea.md
    - Idea.md -> Idea normalizer => idea_normalized.md
    - idea_normalized + (config, ei vielä olemassa) -> concept summarizer => concept_summary.md
    - idea_normalized + concept_summary.md -> epic extractor => epics.md
    - concept_summary + epics.md + idea_normalized -> epic validator => (patched epics.md, validation report)
    - epics.md + concept_summary + idea_normalized + (config, ei vielä olemassa) -> feature extractor => features.md
    - concept_summary + features.md + idea_normalized -> feature validator => (patched features, validation report)
    - features.md + concept_summary + idea_normalized -> task builder => tasks.md
    - tasks.md + concept_summary + idea_normalized -> task validator => (patched tasks tms)
    - into-WPs
    - work-WP


1. imagine → normalize → concept

2. codebase-context

3. epics backlog + validate

4. features backlog + validate

5. existing-solution-map (for EPIC-001)

6. task-builder (EPIC-001) + task-validator

7. repeat 5–6 for each epic

8. into-WPs → work-WP

9. execute WPs
